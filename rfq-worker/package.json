{
  "name": "nixia-fabric-inquiry-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Nixia Fabric — inquiry Worker (contact + RFQ forms, Cloudflare native email)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "wrangler": "^3.90.0"
  }
}
