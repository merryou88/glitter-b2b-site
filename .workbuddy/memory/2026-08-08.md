# 2026-08-08 工作日志

## 首页应用场景卡片图片更新
- 4 张应用场景卡片从渐变色占位符替换为实际产品图片
- 图片路径：`/shots/application-{shoes,handbags,accessories,fashion-trim}-glitter-fabric.jpg`
- 图片规格：1200×675 JPG，各约 200KB
- 卡片布局、列数、标题、底部描述文字全部保持不变
- 构建通过（12 页 0 错误），开发服务器 localhost:4321

## 轮播按钮颜色调整
- 轮播主按钮（Free Sample / Get Custom Quote）从蓝色 `#16294d` 改为金色 `#c4a035`
- 小圆点当前指示器同步改为金色

## 应用场景卡片图片切换为用户自带图片
- 图片路径从 `/shots/application-*.jpg`（AI 生成）切换为 `/image/home/{Shoes,Handbags,Accessories,Fashion}.png`（用户自带）
- 用户明确指出不要用 AI 生成的图片

## 产品顺序调整
- `products.ts` 中 productCategories 数组顺序：Foil Fabric 在前，Glitter Fabric 在后
- 首页分类卡片渐变色：烫金用金色渐变，格利特用银色渐变

## 首页叙事逻辑重构（对标 ecowoodplate.com）
- 移除面料品种区块（Fabric Categories）、工厂能力区块、Why Choose Us 区块、旧优势条
- 新模块顺序：①导航 → ②Hero轮播 → ③4应用场景卡片 → ④4优势图标 → ⑤主推产品预览 → ⑥工厂信任背书 → ⑦FAQ → ⑧底部询盘CTA
- Hero轮播主CTA改为深色高亮（#1a1a1a + 金色边框光晕），所有slide统一Free Sample为主CTA
- Hero轮播添加MP4视频slide基础设施（video字段 + 移动端降级静态图片）
- 优势文案面向B端：Free Sample / Custom Color & Backing / SGS REACH / Factory Direct
- FAQ 使用 details/summary 手风琴，保留 SEO structured data
- 产品列表页添加双维度筛选：By Backing Type + By End-Use Application（客户端JS筛选）
- 构建通过（12 页 0 错误）

## 导航重构：主导航改为应用场景维度，工艺/底布降为页面内筛选器
- **products.ts 数据模型更新**：
  - Product 新增 `craft`（Chunky Glitter / Hot-Stamping Foil / Glitter + Hot-Stamping Foil Composite）
  - Product 新增 `backingCategory`（Non-woven backing / Elastic PU backing）
  - Product 新增 `endUse` 数组（多标签：shoes, handbags, accessories, fashion-trims）
  - 新增 `endUseCategories` 常量（4 个场景，含独立 metaTitle/metaDescription/H1）
  - 新增 helper：`getProductsByEndUse()`, `getEndUseBySlug()`, `getEndUseLabel()`
- **Header.astro**：Our Products 下拉菜单改为 4 个 end-use 入口（Shoes/Handbags/Accessories/Fashion），移动端展开式子菜单
- **首页 index.astro**：4 张应用场景卡片改为可点击链接，hover 显示金色 "View Fabrics →" 浮层
- **新增 EndUseListing.astro 组件**：完整的场景分类页面，含左侧筛选侧边栏（Filter by Craft + Filter by Backing）、产品网格、跨场景链接
- **[slug].astro 更新**：getStaticPaths 同时生成产品详情路径和 end-use 分类路径；条件渲染 EndUseListing 或产品详情模板
  - 产品详情页新增 "Suitable for" 标签区域（链接到对应场景页）
  - 产品详情页添加 canonical 标签
  - CTA 统一指向 /sample-request 和 /quote-request
- **products/index.astro 重写**：左侧筛选侧边栏（Craft + Backing），场景快速入口 chips
- **SEO 规则**：
  - 场景分类页面独立 meta title/description/H1，同时包含 glitter 和 hot-stamping foil 关键词
  - 筛选器 URL 参数（?craft=xxx）通过 JS 动态注入 `<meta name="robots" content="noindex, follow">`
  - 产品详情页设置唯一 canonical 标签
- 构建通过（16 页 0 错误），开发服务器 localhost:4321
- 注意：Astro 配置 `trailingSlash: 'always'`，dev server 访问需带尾斜杠

## 全站专业术语统一规范（对标 hoepke.de 欧洲纺织行业标准）
- **覆盖范围**：13 个源文件全部完成术语替换
- **标准工艺/产品名**：Glitter Fabric → Chunky Glitter Synthetic Leather；Foil Fabric → Hot-Stamping Foil Finish
- **按钮统一文案**：Free Sample → Request Free Swatch；Get Custom Quote → Get Custom Quotation；View All Products → Discover Fabrics
- **合规术语**：SGS Tested → SGS Certified；SGS / REACH Compliant 保持不变
- **能力标签**：OEM Production → Factory Direct Supply；Custom Color Development → Custom Color Matching；Custom Backing Material → Custom Backing Options
- **禁用词黑名单**：bronzing, bronze, "PU Fabric"（作主标题）, view more, click here, cloth
- **关键 Bug 修复**：index.astro 第258行 `product!.category === "Foil Fabric"` 未同步更新，会导致主推产品卡片渐变色显示错误 → 已修正为 `"Hot-Stamping Foil Finish"`
- **类型系统**：products.ts Category 类型从 `"Glitter Fabric" | "Foil Fabric"` 改为 `"Hot-Stamping Foil Finish" | "Chunky Glitter Synthetic Leather"`
- 4 轮 Grep 验证扫描，所有残留旧术语已清除
- 最终构建：16 页 0 错误 0 警告，9.78s 完成

## 恢复 Hero 轮播下方信任条
- 用户反馈轮播图下方的特征描述条（24h RFQ Response / 3-7d Sample Plan / OEM/ODM Custom Projects / FOB/CIF Export Support）被删除
- 在 `index.astro` 的 `HeroCarousel` 与 `Applications` 之间重新插入 `hero-trust` 区块
- 按全站术语规范将 "OEM/ODM" 更新为 "Factory Direct"，其余三项保持原意
- 添加响应式样式：桌面 4 列，平板不变，手机 2 列
- 构建通过：16 页 0 错误 0 警告

## Why Choose Us 区块文案优化
- eyebrow: "Why Choose Us" → "WHY US"
- h2: "Built for B2B Buyers" → "Your Reliable Manufacturing Partner"
- 新增灰色辅助描述文本
- 卡片2描述: "Pantone matching" → "Pantone color-matching"
- 卡片3描述: "on request" → "upon request"
- 卡片4描述: "Manufacturer, not trading company" → "Original manufacturer, not a trading company"
- 构建通过：16 页 0 错误

## 全站行动按钮统一金色
- `global.css` 按钮变体全部改为金色系：
  - `btn--dark`：蓝色 → 金色填充（同 primary）
  - `btn--secondary`：白色边框 → 金色边框+金色文字
  - `btn--outline`：蓝色边框 → 金色边框+金色文字
- `HeroCarousel.astro` 轮播按钮：
  - primary：`#1a1a1a` 深色 → `var(--color-accent)` 金色填充
  - outline：白色边框 → 金色边框+金色文字
- 影响范围：首页、产品详情页、工厂页、关于页、博客页、场景列表页的所有 CTA 按钮
- Logo 图标渐变保留蓝色（品牌元素，非行动按钮）
- 构建通过：16 页 0 错误
