# 2026-08-09 工作日志

## 全站优化（对标 solamni.com）— 7 大模块实施

### 1. 首页产品预览板块重构（Task #41）
- 删除旧的 4 张产品卡片，替换为 2 张核心工艺卡片
- 卡片1：Hot-Stamping Foil Finish — 图片 `/images/preview_hotstamping_foil.jpg`（800x800, 146KB）
- 卡片2：Chunky Glitter Synthetic Leather — 图片 `/images/preview_chunky_glitter.jpg`（800x800, 204KB）
- 板块标题："Explore our core synthetic-leather collections for footwear & accessories."
- CSS：新增 `.collection-card` 样式系列，移除旧的 `.featured-card`

### 2. 产品列表页双筛选重构（Task #42）
- 旧筛选器：Filter by Craft + Filter by Backing
- 新筛选器：Filter by End-Use（Shoes/Handbags/Accessories）+ Filter by Craft
- 底材筛选移除，下沉至产品详情页定制模块
- `data-backing` 属性替换为 `data-enduse`（逗号分隔数组）
- JS 逻辑更新：`activeBacking` → `activeEndUse`，匹配逻辑改为 `includes()` 数组检查
- URL 参数从 `?backing=` 改为 `?enduse=`
- noindex 逻辑保留：带筛选参数的 URL 设置 `noindex, follow`

### 3. 产品详情页图片生成（Task #43）
- `detail_hotstamping_main.jpg`（1200x1200, 323KB）— 热烫箔面料主图
- `detail_glitter_main.jpg`（1200x1200, 350KB）— 格利特面料主图
- `backing_elastic_knit.jpg`（800x800, 77KB）— 弹力针织底材背面图
- `backing_pu_coated.jpg`（800x800, 65KB）— PU 涂层底材背面图
- 色卡使用 CSS 颜色圆点方案（避免过度消耗积分）

### 4. 产品详情页定制参数模块（Task #44）
- 新增定制参数区块，替代旧的 Applications + Customization 双栏
- 模块包含：Backing Material（带参考图）+ Color Selection + Thickness & Width
- 底材选项卡：Elastic Knitted / PU Coated，各带 800x800 参考图
- 色卡选择网格：CSS 颜色圆点 + 名称，支持 Custom Color Matching 选项
- 厚度/门幅：从产品规格数据读取，显示标准值 + "Custom available upon request"
- 产品主图从占位色块替换为真实图片路径
- 所有 CTA 链接更新：`/sample-request` → `/sample-policy`，`/quote-request` → `/contact`

### 5. 新增 3 个独立页面（Task #45）
- `/sample-policy` — 样品政策页：4 步流程、样品包含内容、FAQ（4 项）、CTA
- `/certifications` — 证书页：SGS + REACH 报告卡片（支持 PDF 下载）、4 步质量控制流程
- `/contact` — 询盘表单：公司/姓名/邮箱/产品需求/用途 5 字段，mailto 提交
- 所有页面含独立 meta title/description、面包屑、JSON-LD 结构化数据

### 6. 导航/页脚/博客/SEO 全站更新（Task #46）
- Header 导航：新增 Contact 入口；CTA 按钮链接更新
- Footer 导航：新增 Resources 区块（Sample Policy/Certifications/Contact）；Applications 链接修正为 end-use 路由
- 全站 CTA 链接统一：`/sample-request` → `/sample-policy`，`/quote-request` → `/contact`
- 影响文件：Header/Footer/HeroCarousel/index/about/factory/blog/EndUseListing/products/index/products/[slug]
- 博客标签更新：Glitter-Leather, Hot-Stamping-Foil, Material-Guide, Compliance
- 博客文章摘要增加产品内链引导

### 构建验证
- 最终构建：19 页 0 错误 0 警告，13.40s
- Sitemap 包含全部 19 个页面 URL
- 点击深度 ≤3（所有页面从首页可达）

## 全站业务叙事统一更新（Task #47）

### 业务叙事基准
- 工厂注册时间统一为 since 2013，禁止虚构更早时间
- 企业身份：自有格利特/热烫金PU面料工厂 + 外协合作工厂
- 目标客户：全球鞋、手袋、配饰制造商（footwear, handbag and accessory manufacturers worldwide）
- 价值主张：可靠可定制合成面料，支持样品/定制颜色底材/SGS+REACH报告
- 禁止出现：garment服装、furniture家具等非主营品类描述

### 修改文件清单（7 个文件）
1. **about.astro** — Hero h1/desc、Brand Story 4 段（用户指定原文）、Milestones（Early Years→2013）、Focus section 标题+描述、Industry Experience 描述、Global Markets 欧洲和中东描述、CTA 描述
2. **index.astro** — Factory Trust 段落替换为 since 2013+自有工厂+合作工厂叙事；Applications 副标题 "decorative fabrics"→"glitter and foil fabrics"；Fashion 卡片描述 "Garment trims"→"Fashion trims and decorative accents"；FAQ 答案更新（manufacturer 答案加入 partnerships，products 答案去掉 fashion applications）
3. **products/index.astro** — Page hero desc 替换为用户指定导语
4. **sample-policy.astro** — Page hero desc 替换为 since 2013 manufacturer 叙事
5. **contact.astro** — Page hero desc 替换为 since 2013 supplier 叙事
6. **blog/index.astro** — Blog hero desc 替换为统一公司背景描述
7. **factory.astro** — Hero desc、Factory Introduction 3 段、"What We Don't Do" 卡片（移除 "We don't outsource" / "We don't sell other factories' products"，替换为 "No middleman markup" / "Direct factory communication"）
8. **HeroCarousel.astro** — Slide 2 标题 "Decorative Fabric"→"Glitter & Foil Fabric"

### 约束遵守
- 只替换介绍类文本，未改动 SEO meta/JSON-LD、图片路径、筛选逻辑、表单逻辑、页面结构
- 全站 "since 2013" 口径统一
- 自有工厂+外协合作叙事在首页/About/产品总览/Sample Policy/Contact/Blog/Factory 全部一致

### 构建验证
- 19 页 0 错误 0 警告，14.68s

## 深蓝色横幅栏优化（Task #48）

### 修改内容
1. **Header.astro 顶栏文案**：`CHINA GLITTER & FOIL FABRIC MANUFACTURER` → `Glitter & Hot-Stamping Foil Synthetic Leather Manufacturer`（去掉 China 开头）
2. **Header.astro 顶栏右侧联系入口**：新增 5 个可点击链接，左→右排序：
   - WhatsApp（`https://wa.me/8613800000000`，target=_blank）
   - Phone（`tel:+8613800000000`）
   - Email（`mailto:info@nixiafabric.com`）
   - Facebook（`https://www.facebook.com/nixiafabric`，图标链接）
   - LinkedIn（`https://www.linkedin.com/company/nixiafabric`，图标链接）
3. **Header.astro 顶栏 CSS**：`justify-content: flex-start` → `space-between`；新增 `.header__topbar-contacts`、`.header__topbar-contact`、`.header__topbar-social` 样式；响应式（≤1100px 隐藏文字只显示图标，≤640px 缩小间距）
4. **Footer.astro tagline**：去掉 "China" 开头，改为 "Glitter and hot-stamping foil synthetic leather manufacturer..."
5. **Footer.astro 底栏小字**：新增 `<span class="footer__origin">Manufacturer from China</span>`，竖线分隔样式

### 联系方式占位值
- WhatsApp/Phone 号码：`+8613800000000`（占位，待用户替换为真实号码）
- Email：`info@nixiafabric.com`（占位）
- Facebook/LinkedIn：基于 nixiafabric 域名推测（占位）

### 构建验证
- 19 页 0 错误 0 警告，22.76s
- 新增：悬浮按钮脉冲光圈动画（双层绿色 pulse ring，2.4s 循环，弹窗打开时隐藏，prefers-reduced-motion 禁用）

## 全局悬浮联系弹窗组件（Task #49）

### 新建文件
- `src/components/FloatingContact.astro` — 全局悬浮联系弹窗

### 功能
- 固定在浏览器右下角（`position: fixed; bottom: 24px; right: 24px`），z-index: 999
- 圆形触发按钮（56px，深蓝渐变），点击展开弹窗 / 再次点击关闭
- 弹窗标题："Our Contact Options"
- 提示文本："Welcome to inquire about glitter & hot-stamping synthetic leather. Please choose your preferred contact method:"
- 按钮1：Open WhatsApp Chat — `https://wa.me/8613800000000`（新窗口打开，WhatsApp 绿色）
- 按钮2：WeChat Contact — 点击展开 WeChat ID（`NixiaFabric`），含一键复制功能（clipboard API + execCommand fallback）
- 关闭方式：X 按钮 / 再次点击触发按钮 / Escape 键 / 移动端点击背景遮罩
- 无虚假"在线状态"提示
- 响应式：≤640px 弹窗宽度自适应 `calc(100vw - 32px)`，按钮缩小至 50px，显示半透明背景遮罩
- 支持 `prefers-reduced-motion`

### 全局集成
- `Layout.astro` 中 import FloatingContact 并放在 `<Footer />` 之后，所有 19 个页面自动生效

### 构建验证
- 19 页 0 错误 0 警告，16.46s

## FloatingContact 按钮金色统一 + 脉冲光圈动画

### 脉冲光圈（pulse ring）
- `::before` + `::after` 双层绿色光圈 → 改为金色 `var(--color-accent)`
- `@keyframes float-contact-pulse`：scale(1)→scale(1.8)，opacity 0.7→0，2.4s 循环
- 第二层延迟 1.2s，弹窗打开时 `animation: none` 隐藏
- `prefers-reduced-motion` 禁用

### 全部按钮金色统一
- 触发按钮：深蓝渐变 → 金色渐变 `var(--color-accent)` → `var(--color-accent-dark)`
- 弹窗头部背景：深蓝 → 金色渐变
- WhatsApp 按钮：`#25D366` → `var(--color-accent)`
- WeChat 按钮：`#07C160` → `var(--color-accent-dark)`
- 复制成功状态：`#25D366` → `var(--color-accent)`
- 触发按钮阴影/悬停阴影：蓝色 rgba → 金色 rgba

## Footer 重构为 EcoWoodPlate 四栏分组式（Task #50）

### 结构
- 移除旧 CTA Bar（"Need a Custom Fabric Solution?" 蓝色横幅）
- 移除旧 4 列零散导航（Products/Company/Resources/Applications）+ badges
- 新 4 栏结构：
  1. **Brand**：Logo + since 2013 简介文案 + Facebook/LinkedIn 社交图标
  2. **Products**：Glitter Synthetic Leather / Hot-Stamping Foil PU / Custom Backing & Color / Request Free Sample
  3. **Company**：About Us / Sample Policy / Certifications / Blog / Get Custom Quote
  4. **Contact**：地址（Dongguan, Guangdong）+ Email（mailto）+ Phone（tel）+ 金色 Request Free Sample CTA 按钮
- 底部版权行：`© 2026 Nixia Fabric Co., Ltd. All rights reserved.` + Privacy Policy 链接

### 约束遵守
- 无 Recommended 产品缩略图模块
- 导航分组归类，不零散堆砌
- Contact 栏突出 Request Free Sample 金色 CTA 按钮
- 移动端 ≤968px 两列，≤640px 单列
- 文案口径与全站叙事统一（since 2013 + 自有工厂+合作工厂）

### 构建验证
- 19 页 0 错误 0 警告，16.81s

## 全站 Swatch→Sample 按钮文案统一（Task #51）

### 规则
- 所有按钮/CTA/导航/标题：统一 "Request Free Sample"（禁止 Swatch）
- 正文描述仅 Sample Policy 页和产品详情页可保留 "swatch"（区分小块色卡和大货样）
- CSS 类名/变量名/代码注释中的 "swatch" 不改（内部代码）

### 修改文件清单（13 文件）
1. **Header.astro** — 2 处 "Request Free Swatch" → "Request Free Sample"
2. **HeroCarousel.astro** — 5 处按钮 + 1 处 slide 描述 "swatches" → "samples"
3. **Hero.astro** — 按钮文本 + href `?inquiry=swatch` → `?inquiry=sample`
4. **index.astro** — 按钮文本 replace_all + FAQ 答案 "swatches" → "samples" + 板块标题 "Swatches" → "Samples" + 正文 "swatches" → "samples"
5. **about.astro** — 按钮文本
6. **EndUseListing.astro** — 按钮文本 + 正文 "swatches" → "samples"
7. **products/index.astro** — 按钮文本 + 正文 "swatch" → "sample"
8. **products/[slug].astro** — 4 处按钮 replace_all；保留正文 "Request swatches for actual color" 和 "Free swatch shipping"
9. **factory.astro** — 按钮文本 + 正文 "swatches" → "samples"
10. **certifications.astro** — 按钮文本
11. **blog/index.astro** — 按钮文本
12. **sample-policy.astro** — h1 + h2 CTA 标题 + 按钮文本改为 "Sample"；保留 FAQ/步骤标题/样品清单中的 "swatch"
13. **contact.astro** — h1 + SEO meta title/description/keywords 中 "swatch" → "sample"
14. **products.ts** — end-use metaDescription 2 处 "swatches" → "samples"；保留 samplePolicy 字段中的 "swatch"

### 构建验证
- 19 页 0 错误 0 警告，14.82s

## 全站主标题/术语修正：Hot-Stamping Foil → Foil-Printed（Task #49）

### 术语变更规则
- 主标题/横幅/导航/SEO meta/正文描述：统一使用 "Foil-Printed Synthetic Leather"（禁止主标题出现 "Hot-Stamping Foil"）
- 产品详情正文可使用 sparkle/sparkly 做修饰（不用于主标题）
- 内部数据值（category/craft/craftOptions/CraftType 类型）保留 "Hot-Stamping Foil" 不改，保障筛选/路由逻辑
- 博客标签（Hot-Stamping-Foil）和 URL slug 保留不改（筛选标识符）
- 页脚导航：Hot-Stamping Foil PU → Foil-Printed PU Synthetic Leather
- 顶栏标语：Glitter & Hot-Stamping Foil Synthetic Leather Manufacturer → Glitter & Foil-Printed Synthetic Leather Manufacturer

### 修改文件清单（16 文件）
1. **Header.astro** — 顶栏标语
2. **Layout.astro** — pageTitle、default description、keywords、JSON-LD org description/knowsAbout/makesOffer
3. **Hero.astro** — eyebrow、h1、subtitle、tag title
4. **HeroCarousel.astro** — slide 1 title、3 处 slide descriptions
5. **Footer.astro** — tagline "hot-stamping foil" → "foil-printed"、nav link "Hot-Stamping Foil PU" → "Foil-Printed PU Synthetic Leather"
6. **index.astro** — JSON-LD name/desc、collection card label/title/desc/alt、SEO meta desc/keywords、FAQ answers、factory trust 段落、final CTA 标题（修复 typo "Samplees" → "Samples"）
7. **about.astro** — SEO meta desc、JSON-LD knowsAbout、h2 focus 标题、focus 产品名、milestone desc、brand story 正文、industry experience 正文、global markets 正文、CTA 描述
8. **factory.astro** — SEO meta desc、JSON-LD desc、capability title、h2 intro 标题、production list item、manufacturing capability 正文、factory intro 正文
9. **products/index.astro** — JSON-LD name/desc、SEO meta title/desc/keywords、h1、page hero desc
10. **products.ts** — 注释头、4 个 end-use category 的 h1/metaTitle/metaDescription/description/alt、collection name/desc/seoKeywords、4 个产品 name + description
11. **blog/index.astro** — 2 篇文章 title/excerpt、JSON-LD desc、SEO meta desc/keywords、blog notice 正文
12. **certifications.astro** — SGS cert desc、page hero desc
13. **sample-policy.astro** — sample includes 文本、SEO meta desc
14. **contact.astro** — product options label、page hero desc
15. **EndUseListing.astro** — SEO keywords、body text
16. **FloatingContact.astro** — popup 描述文本
17. **global.css** — 文件头注释

### 保留 "Hot-Stamping Foil" 的位置（数据逻辑）
- products.ts: CraftType 类型定义、category 类型、craftOptions 数组、各产品的 category/craft 字段值
- [slug].astro: `p.category === "Hot-Stamping Foil Finish"` 条件渲染
- blog/index.astro: 文章 slug、tags 数组、categories 筛选数组、tag 比较逻辑

### 构建验证
- 19 页 0 错误 0 警告，14.83s

## FloatingContact 悬浮按钮 WhatsApp 绿色改版

### 改动内容
- 触发按钮图标：聊天气泡 SVG → WhatsApp 图标 SVG（fill="currentColor"，30px）
- 触发按钮主色：金色渐变 → WhatsApp 绿色 `#25D366`（hover `#20bd5a`）
- 脉冲光圈：金色 `var(--color-accent)` → 绿色 `#25D366` 半透明
- 弹窗头部背景：金色渐变 → 绿色渐变 `#25D366` → `#20bd5a`
- WhatsApp 按钮：金色 `var(--color-accent)` → `#25D366`（hover `#20bd5a`）
- WeChat 按钮：金色 `var(--color-accent-dark)` → 微信绿 `#07C160`（hover `#06ad55`）
- 复制按钮：金色边框/文字 → 绿色 `#25D366` 边框/`#20bd5a` 文字
- 复制成功状态：金色 → `#25D366`
- 触发按钮 open 状态：金色深色渐变 → `#20bd5a`

### 设计意图
- WhatsApp 绿色突出为主要联系入口，WeChat 微信绿为辅助
- 不使用金色，避免与网站普通 CTA 按钮混淆

### 构建验证
- 19 页 0 错误 0 警告，22.08s

## 联系表单重构：mailto → Cloudflare Pages Functions + Turnstile

### 删除的旧方案
- `action="mailto:info@nixiafabric.com"` mailto 表单（弹出本地邮件客户端，外贸不可用）
- Product Needs / Use Case 下拉选择器（简化为 Subject 文本输入）
- CTAButton import（contact 页不再使用）

### 新建文件
1. **`functions/api/contact.js`** — Cloudflare Pages Function
   - 接收 JSON POST：name / company / email / subject / message + cf-turnstile-response
   - 验证 Cloudflare Turnstile token（siteverify API）
   - 组装 HTML + 纯文本邮件
   - 通过 Resend API 投递到业务邮箱（reply_to 设为客户邮箱）
   - CORS 白名单：nixiafabric.com / www.nixiafabric.com
   - 环境变量：TURNSTILE_SECRET_KEY / RESEND_API_KEY / CONTACT_TO_EMAIL / CONTACT_FROM_EMAIL
   - 仅部署到 Cloudflare Pages 后生效，本地 dev 访问 /api/contact 返回 404（正常）

2. **`src/pages/thank-you.astro`** — 感谢页面
   - 绿色勾选图标 + "Thank You for Reaching Out!" 标题
   - WhatsApp 按钮（#25D366 绿色）+ Browse Our Fabrics 按钮
   - 24h 响应承诺 + 邮箱联系方式
   - 响应式布局

### 修改文件
3. **`src/pages/contact.astro`** — 表单重构
   - 表单字段：Name / Company / Email / Subject / Message（全部 required）
   - 移除 mailto action，改为 novalidate + JS fetch POST
   - Cloudflare Turnstile widget（data-sitekey 占位值，需替换为真实 key）
   - 提交按钮：加载状态 spinner 动画 + disabled
   - 错误处理：红色 .form-error 横幅，内联显示后端返回的错误信息
   - 成功：`window.location.href = "/thank-you/"`
   - 前端验证：必填检查 + email 格式正则 + Turnstile token 检查

### 约束遵守
- 不使用 Formspree 等第三方表单服务
- 不唤起本地邮件客户端
- 本地 dev 表单提交报错属于正常（Pages Functions 仅在部署后生效）
- 其余页面布局、WhatsApp 悬浮按钮、四栏页脚、业务文案不变

### 构建验证
- 20 页（19 + thank-you）0 错误，36.81s

## Turnstile 反机器人验证交互修复

### 改动内容
- 提交按钮初始状态 `disabled`，用户完成验证后自动启用
- Turnstile widget 添加 `data-callback` / `data-expired-callback` / `data-error-callback` 属性
- 新增 `#turnstile-hint` 提示文本 "Please complete the anti-bot verification."（验证未完成时显示，完成后隐藏）
- 新增 3 个全局回调函数：`onTurnstileSuccess` / `onTurnstileExpired` / `onTurnstileError`（通过 `window` 暴露，解决 Astro module scope 问题）
- `onTurnstileError` 额外调用 `showError()` 显示红色错误横幅
- `.form-turnstile-hint` CSS：琥珀色文字 + 警告图标 SVG
- `setLoading()` 内不再额外覆盖 disabled 状态（loading 结束后恢复 Turnstile 验证状态）

### 约束
- 本地 dev Turnstile 渲染异常属于正常，部署到 Cloudflare Pages 后生效
- 表单字段不变：Name* / Company* / Email* / Subject* / Message*
- 成功跳转 /thank-you，失败内联错误提示，不使用 alert

### 构建验证
- 20 页 0 错误，32.97s
