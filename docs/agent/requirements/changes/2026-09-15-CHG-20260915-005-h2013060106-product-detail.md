---
change_id: CHG-20260915-005-h2013060106-product-detail
date: 2026-09-15
status: implemented
requirements:
  - REQ-DATA-003
  - REQ-DATA-004
  - REQ-PRODUCT-001
  - REQ-PRODUCT-002
  - REQ-PRODUCT-003
  - REQ-PRODUCT-018
modules:
  - content-data
  - product-catalog
  - inquiry-forms
---

# H2013060106 Dense Dot Foil 产品详情页

## 变更原因
根据 `面料独立站产品数据.xlsx` 新增 H2013060106 产品详情页，并按图片内容整理详情图顺序。

## 变更后
新增公开产品 `non-shedding-glitter-suede-look-laser-foil-fabric`。页面使用 Excel 中确认的英文标题、SEO 标题、Meta Description、应用、关键特征和规格：150cm、130g、MOQ 100m、16 colors、Slight Stretch、210T、75D*75DD、Foil、Foil embossing 和 Suede-look fabric。

详情图按内容重新排序并连续编号为 `x1`–`x12`。原 `x12.jpg` 实际为 Product Applications 拼图，已移至 `application/1.jpg`。对应 WebP 已生成，PNG/JPG 原图保持无水印，除首张主图外的主图和详情图 WebP 每张使用 3 个低透明度、逆时针 45 度倾斜 `nixiafabric.com` 水印；Application PNG/WebP 不加水印。

详情页遵循统一模板，不显示独立 `Product Description` 模块。

## 验收条件
- `/products/non-shedding-glitter-suede-look-laser-foil-fabric/` 能生成并展示。
- 详情图连续编号为 `x1`–`x12`，且按内容顺序展示。
- Application 拼图独立引用 `application/1.jpg` 和 `application/1.webp`。
- 页面显示 Excel 已确认参数，不编造纤维成分或未确认交期。
- 页面包含 Key Features、Recommended Applications、Why Choose、Ready Stock & Custom Development、Sample CTA 和 FAQ。
- `Specifications Table for B2B Buyers` 前不显示 `Product Description` 模块。

## 实现位置
`src/data/allProducts.js`
`src/components/ProductDetail.astro`
`public/images/products/H2013060106/`

## 验证结果
已完成图片重命名、Application 图片分离和 WebP 生成；已执行 `npm run build` 与 `npm run validate:products`，均通过。

## 来源
用户 2026-09-15 需求；参数来源为 `/Users/ouyanxia/Documents/nixiafabricoversea/products-data/面料独立站产品数据.xlsx` 中 H2013060106。
