---
change_id: CHG-20260922-001-h2013060101-image-path
date: 2026-09-22
status: implemented
requirements:
  - REQ-DATA-001
  - REQ-DATA-004
  - REQ-DATA-005
modules:
  - content-data
  - product-catalog
---

# H2013060101 图片目录改为 SKU 路径

## 变更原因
H2013060101 产品图片文件夹已从产品名称目录调整为 SKU 目录 `H2013060101`，代码需要同步更新图片路径。

## 变更前
- H2013060101 对应产品仍引用 `/images/products/full-print-hot-stamping-spandex-milk-silk/`。
- 主产品数据、博客图片、应用页图片和旧版 `products.json` 使用旧产品名称目录。

## 变更后
- 主图、详情图、应用图和博客引用统一改为 `/images/products/H2013060101/`。
- `src/data/allProducts.js` 的 `galleryMainPath` 和 `detailImagePath` 改为 SKU 目录。
- 新目录中的 `.DS_Store` 系统文件已清理。
- 产品名称、slug、SKU、页面内容和业务字段保持不变。

## 验收条件
- 主站产品详情页、博客和应用页引用的 H2013060101 图片全部来自新目录。
- `src/data`、`src/pages` 和 `docs/agent` 中不再出现旧图片目录引用。
- 产品校验和构建通过，且新目录图片资源全部存在。

## 影响范围
产品图片路径、博客图片路径、应用页图片路径和旧版产品数据图片路径。

## 实现位置
- `src/data/allProducts.js`
- `src/data/blogArticles.js`
- `src/data/products.json`
- `src/pages/applications/[slug].astro`
- `src/pages/applications/index.astro`
- `public/images/products/H2013060101/`

## 验证结果
- `npm run build` 通过，构建 40 个静态页面。
- `npm run validate:products` 通过，10 个公开产品校验成功。
- `src/`、`public/` 和当前需求文档中已无旧产品名称图片路径；变更记录中的“变更前”说明保留旧路径作为审计信息。
- 构建产物包含 `/images/products/H2013060101/` 新路径，H2013060101 图片资源均可读取。
- `git diff --check` 通过；构建中的 `/api/rfq-submit` GET handler warning 为既有 Astro 路由警告。

## 来源
- 用户 2026-09-22 提供的 H2013060101 图片目录变更信息。
