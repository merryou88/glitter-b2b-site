---
/**
 * index.astro — Nixia Fabric Home Page
 * Solution-oriented narrative (inspired by ecowoodplate.com)
 *
 * Module order: Hero → Our Main Fabric Series → Applications →
 *               Why Choose Nixia Fabric → Our Factory → Factory Trust →
 *               FAQ → Final CTA
 */
import Layout from "@layouts/Layout.astro";
import HeroCarousel from "@components/HeroCarousel.astro";
import CTAButton from "@components/CTAButton.astro";

// WebPage structured data for SEO
const webPageData = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  name: "Nixia Fabric - Glitter & Thermal Foil Gulelite Fabric | OEM ODM Manufacturer & Wholesale Supplier",
  description:
    "Custom low MOQ glitter and thermal foil gulelite fabric supplier for Southeast Asia and Middle East buyers. Free sample available, OEM ODM support, and fast quote response.",
  url: "https://nixiafabric.com",
};

// FAQPage structured data for rich snippets
const faqData = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: [
    {
      "@type": "Question",
      name: "Do you offer free samples?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes, we provide free fabric samples. The buyer only covers international shipping cost. Contact us to request samples for your project.",
      },
    },
    {
      "@type": "Question",
      name: "Can I get custom colors and custom backing?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. We offer Custom Color Matching including Pantone matching, and Custom Backing Options including elastic, PU, non-woven, and Oxford.",
      },
    },
    {
      "@type": "Question",
      name: "Are your fabrics SGS Certified and REACH Compliant?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. Our fabrics are SGS Certified and REACH Compliant. Test reports are available upon request.",
      },
    },
    {
      "@type": "Question",
      name: "Is Nixia Fabric a manufacturer or a trading company?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Nixia Fabric is a manufacturer — not a trading company. We own our in-house factory for glitter and foil-printed PU materials, with stable partnerships with other qualified material manufacturers.",
      },
    },
    {
      "@type": "Question",
      name: "What products do you manufacture?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "We manufacture glitter synthetic leather and foil-printed PU materials for shoes, handbags, and accessories. Multiple backing options and custom colors available.",
      },
    },
    {
      "@type": "Question",
      name: "What is your minimum order quantity?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Our standard MOQ varies by product type and customization level. We also support sampling and trial orders for new projects — contact us with your requirements for detailed MOQ information.",
      },
    },
    {
      "@type": "Question",
      name: "Can you provide samples before bulk order?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes, we provide free fabric samples so you can evaluate material quality, color and texture before placing a bulk order. The buyer only covers international shipping cost.",
      },
    },
    {
      "@type": "Question",
      name: "Can you customize colors and backing?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. We offer full custom color matching including Pantone references, and multiple backing options including elastic, PU, non-woven, and Oxford cloth to meet your specific application needs.",
      },
    },
    {
      "@type": "Question",
      name: "Do you support international shipping?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes, we support international shipping with FOB and CIF trade terms. We have experience exporting to global footwear, handbag and accessory manufacturers.",
      },
    },
    {
      "@type": "Question",
      name: "Are your materials SGS certified and REACH compliant?",
      acceptedAnswer: {
        "@type": "Answer",
        text: "Yes. All our glitter and foil-printed synthetic leather materials are SGS Certified and REACH Compliant. Test reports are available upon request to support your quality assurance requirements.",
      },
    },
  ],
};

const hotStampingFoilProducts = [
  {
    title: "Metallic Foil-Printed Synthetic Leather",
    description: "Reflective surface for stage and party projects.",
    image: "/images/products/metallic-foil-fabric/01.jpg",
    alt: "Metallic Foil-Printed Synthetic Leather",
    href: "/products",
  },
  {
    title: "Gold Foil-Printed Synthetic Leather",
    description: "Premium gold finish with strong visual impact.",
    image: "/images/products/gold-foil-fabric/01.jpg",
    alt: "Gold Foil-Printed Synthetic Leather",
    href: "/products",
  },
  {
    title: "Silver Foil-Printed Synthetic Leather",
    description: "Bright silver surface for trims and decor.",
    image: "/images/products/silver-foil-fabric/01.jpg",
    alt: "Silver Foil-Printed Synthetic Leather",
    href: "/products",
  },
  {
    title: "Chunky Glitter + Foil-Printed Composite Fabric",
    description: "Composite surface for heavier decorative use.",
    image: "/images/products/oxford-glitter-fabric/01.jpg",
    alt: "Chunky Glitter + Foil-Printed Composite Fabric",
    href: "/products",
  },
];

const glitterProducts = [
  {
    title: "Elastic Chunky Glitter Synthetic Leather",
    description: "Stretchable glitter fabric for footwear and bags.",
    image: "/images/products/elastic-glitter-fabric/01.jpg",
    alt: "Elastic Chunky Glitter Synthetic Leather",
    href: "/products",
  },
  {
    title: "Elastic PU Chunky Glitter Synthetic Leather",
    description: "Durable PU base with bonded glitter finish.",
    image: "/images/products/pu-glitter-fabric/01.jpg",
    alt: "Elastic PU Chunky Glitter Synthetic Leather",
    href: "/products",
  },
  {
    title: "Non-woven Chunky Glitter Synthetic Leather",
    description: "Lightweight option for accessories and decor.",
    image: "/images/products/non-woven-glitter-fabric/01.jpg",
    alt: "Non-woven Chunky Glitter Synthetic Leather",
    href: "/products",
  },
  {
    title: "Chunky Glitter + Foil-Printed Composite Fabric",
    description: "Composite fabric with glitter and foil finish.",
    image: "/images/products/oxford-glitter-fabric/01.jpg",
    alt: "Chunky Glitter + Foil-Printed Composite Fabric",
    href: "/products",
  },
];

// Applications — 4 end-use scenario cards
const applications = [
  {
    title: "Fabrics for Bags & Shoes",
    description: "Uppers, clutches, hair-accessories & fashion trims.",
    image: "/image/home/Shoes.png",
    alt: "Fabrics for bags and shoes",
    href: "/products",
  },
  {
    title: "Fabrics for Stage & Costume",
    description: "Stagewear, dance costumes, cosplay and carnival performance outfits.",
    image: "/image/home/Handbags.png",
    alt: "Fabrics for stage and costume",
    href: "/products",
  },
  {
    title: "Fabrics for Night-Club & KTV Upholstery",
    description: "KTV booth & nightclub wall soft-pack interior decoration. Fire-retardant optional.",
    image: "/image/home/Accessories.png",
    alt: "Fabrics for night-club and KTV upholstery",
    href: "/products",
  },
  {
    title: "Fabrics for Toys, Crafts & Festive Decor",
    description: "Toy covers, DIY craft materials, party backdrop & Christmas decorative projects.",
    image: "/image/home/Fashion.png",
    alt: "Fabrics for toys, crafts and festive decor",
    href: "/products",
  },
];

const whyNixiaBenefits = [
  {
    title: "Flexible MOQ",
    desc: "Start small. Flexible MOQ for fabric trial orders and mass-production runs.",
    icon: "boxes",
  },
  {
    title: "Fast Sampling",
    desc: "Fabric samples ready within 3-7 working days.",
    icon: "swatch",
  },
  {
    title: "Compliance Support",
    desc: "SGS, fire-retardant reports or buyer-specific test documents can be arranged by project.",
    icon: "certificate",
  },
  {
    title: "Global Shipping",
    desc: "FOB, CIF or buyer-forwarder handover can be arranged per order.",
    icon: "shipping",
  },
];

// Our Factory entry — 3 trust entry cards
const factoryEntries = [
  {
    title: "Production Line",
    image: "/image/process/hot_stamping_foil_transfer.jpg",
    alt: "Foil-printed synthetic leather production line in factory",
    href: "/factory",
  },
  {
    title: "Quality Control",
    image: "/image/process/on_line_visual_inspection.jpg",
    alt: "QC inspection station for fabric quality control",
    href: "/factory",
  },
  {
    title: "Warehouse",
    image: "/image/process/final_packing.jpg",
    alt: "Finished goods warehouse with packed fabric rolls",
    href: "/factory",
  },
];

// Factory trust badges
const trustBadges = [
  { label: "Guangzhou Base" },
  { label: "Southeast Asia" },
  { label: "Middle East" },
  { label: "Factory Backed" },
];
---

<Layout
  title="Nixia Fabric - Glitter & Thermal Foil Gulelite Fabric | OEM ODM Manufacturer & Wholesale Supplier"
  description="Custom low MOQ glitter and thermal foil gulelite fabric supplier for Southeast Asia and Middle East buyers. Free sample available, custom colors, and OEM ODM support."
  keywords="glitter synthetic leather, foil-printed synthetic leather, synthetic leather manufacturer, China fabric factory, custom synthetic leather, OEM ODM fabric, SGS REACH compliant"
  structuredData={[webPageData, faqData]}
>
  <!-- ==================== HERO CAROUSEL ==================== -->
  <HeroCarousel />

  <!-- ==================== HERO TRUST BAR ==================== -->
  <section class="hero-trust" aria-label="Business advantages">
    <div class="container">
      <div class="hero-trust__grid">
        <div class="hero-trust__item">
          <span class="hero-trust__value">24h</span>
          <span class="hero-trust__label">RFQ Response</span>
        </div>
        <div class="hero-trust__item">
          <span class="hero-trust__value">Factory Direct</span>
          <span class="hero-trust__label">Own Production Capability</span>
        </div>
        <div class="hero-trust__item">
          <span class="hero-trust__value">SGS</span>
          <span class="hero-trust__label">Certified Materials</span>
        </div>
        <div class="hero-trust__item">
          <span class="hero-trust__value">OEM & ODM</span>
          <span class="hero-trust__label">Custom Solutions</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ==================== OUR MAIN FABRIC SERIES ==================== -->
  <section class="section" id="main-fabric-series">
    <div class="container">
      <div class="section-header">
        <span class="eyebrow">HOT SELLING</span>
        <h2>Popular Products</h2>
        <p>Bulk OEM &amp; ODM glitter and hot-stamping foil fabrics. Clear MOQ, sample support and export-ready packing.</p>
      </div>

      <div class="hot-products-group">
        <div class="hot-products-group__head">
          <h3>Hot-Stamping Foil Fabrics</h3>
          <a href="/products" class="hot-products-group__all">View All →</a>
        </div>
        <div class="grid grid--4 fabric-series-grid">
          {hotStampingFoilProducts.map((item) => (
            <a href={item.href} class="fabric-card">
              <div class="fabric-card__visual">
                <span class="fabric-card__tag">BUYER PICK</span>
                <img src={item.image} alt={item.alt} width="600" height="600" loading="lazy" decoding="async" />
              </div>
              <div class="fabric-card__body">
                <h3>{item.title}</h3>
                <p>{item.description}</p>
                <div class="fabric-card__meta">
                  <span>MOQ 200m</span>
                  <span>Lead time 15-20 days</span>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>

      <div class="hot-products-group">
        <div class="hot-products-group__head">
          <h3>Glitter Fabrics</h3>
          <a href="/products" class="hot-products-group__all">View All →</a>
        </div>
        <div class="grid grid--4 fabric-series-grid">
          {glitterProducts.map((item) => (
            <a href={item.href} class="fabric-card">
              <div class="fabric-card__visual">
                <span class="fabric-card__tag">BUYER PICK</span>
                <img src={item.image} alt={item.alt} width="600" height="600" loading="lazy" decoding="async" />
              </div>
              <div class="fabric-card__body">
                <h3>{item.title}</h3>
                <p>{item.description}</p>
                <div class="fabric-card__meta">
                  <span>MOQ 200m</span>
                  <span>Lead time 15-20 days</span>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ==================== APPLICATIONS (4 scenario cards) ==================== -->
  <section class="section section--alt" id="applications">
    <div class="container">
      <div class="section-header">
        <span class="eyebrow">WHAT WE MAKE</span>
        <h2>Product Series</h2>
        <p>Factory-direct wholesale glitter &amp; hot-stamping foil fabrics for importers, distributors and bulk-order buyers.</p>
      </div>

      <div class="grid grid--4 applications-grid">
        {applications.map((app) => (
          <a href={app.href} class="app-card">
            <div class="app-card__visual">
              <img
                src={app.image}
                alt={app.alt}
                width="1200"
                height="675"
                loading="lazy"
                decoding="async"
              />
              <div class="app-card__overlay">
                <span class="app-card__cta">View Fabrics →</span>
              </div>
            </div>
              <div class="app-card__body">
                <h3>{app.title}</h3>
                <p>{app.description}</p>
                <span class="app-card__link">View Products ›</span>
              </div>
            </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ==================== WHY NIXIA ==================== -->
  <section class="section" id="why-nixia">
    <div class="container">
      <div class="why-nixia-split">
        <div class="why-nixia-copy">
          <span class="eyebrow">WHY NIXIA</span>
          <h2>Your Reliable Fabric Manufacturing Partner</h2>
          <p>From raw base fabric to finished glitter &amp; hot-stamping material — full-service manufacturing with documented quality control.</p>

          <div class="why-nixia-advantages" aria-label="Core advantages">
            {whyNixiaBenefits.map((item) => (
              <div class="why-nixia-advantage">
                <div class="why-nixia-advantage__icon" aria-hidden="true">
                  {item.icon === "boxes" && (
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M4 8l8-4 8 4-8 4-8-4z" />
                      <path d="M4 8v8l8 4 8-4V8" />
                      <path d="M12 12v8" />
                    </svg>
                  )}
                  {item.icon === "swatch" && (
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M6 5h10l2 5-6 9H8L6 5z" />
                      <path d="M8 9h6" />
                      <path d="M9 12h4" />
                    </svg>
                  )}
                  {item.icon === "certificate" && (
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M7 4h10v14H7z" />
                      <path d="M9 8h6" />
                      <path d="M9 12h4" />
                      <path d="M14 15l2 4-2-1-2 1 2-4z" />
                    </svg>
                  )}
                  {item.icon === "shipping" && (
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                      <path d="M3 15h11l2-5H7L5 7H3z" />
                      <path d="M14 15h5l-1-4h-4z" />
                      <circle cx="8" cy="18" r="1.2" />
                      <circle cx="17" cy="18" r="1.2" />
                    </svg>
                  )}
                </div>
                <div>
                  <h3>{item.title}</h3>
                  <p>{item.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>

        <div class="why-nixia-media">
          <img src="/image/process/hot_stamping_foil_transfer.jpg" alt="Factory workshop for fabric manufacturing" loading="lazy" />
          <div class="why-nixia-badges" aria-label="Factory badges">
            <span class="why-nixia-badge-chip">SGS Test Available</span>
            <span class="why-nixia-badge-chip">Verified Factory</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ==================== MANUFACTURING CAPABILITIES ==================== -->
  <section class="section section--alt" id="manufacturing-capabilities">
    <div class="container">
      <div class="capabilities-shell">
        <div class="capabilities-shell__title">MANUFACTURING CAPABILITIES</div>

        <div class="capability-bar" aria-label="Manufacturing capabilities">
          <div class="capability-item">
            <span class="capability-item__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                <path d="M5 8h7l1 3H5z" />
                <path d="M12 7h6l1 4h-6z" />
                <path d="M5 11v6h14v-6" />
                <path d="M9 15h6" />
                <path d="M9 18h4" />
              </svg>
            </span>
            <div>
              <h3>Custom Foil Stamping</h3>
              <p>Custom metallic foil patterns &amp; colors</p>
            </div>
          </div>

          <div class="capability-item">
            <span class="capability-item__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                <path d="M5 16c1.5-5 3.5-8 6-8s4 3 8 8" />
                <path d="M7 13l2-3 2 4 2-5 2 6" />
                <path d="M4 19h16" />
              </svg>
            </span>
            <div>
              <h3>Glitter Coating</h3>
              <p>Fine-particle glitter surface treatment</p>
            </div>
          </div>

          <div class="capability-item">
            <span class="capability-item__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 7h7l4 4v6H4z" />
                <path d="M11 7v4h4" />
                <path d="M7 14c2-2 5-3 8-4" />
                <path d="M15 10l2 1.5" />
              </svg>
            </span>
            <div>
              <h3>Custom Design</h3>
              <p>Custom pattern, color and base fabric selection</p>
            </div>
          </div>

          <div class="capability-item">
            <span class="capability-item__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                <path d="M6 7h12" />
                <path d="M7 7l2 10h6l2-10" />
                <path d="M9 11h6" />
                <path d="M10 15h4" />
              </svg>
            </span>
            <div>
              <h3>Surface Finishing</h3>
              <p>Anti-drop-powder &amp; wear-resistant treatment</p>
            </div>
          </div>

          <div class="capability-item">
            <span class="capability-item__icon" aria-hidden="true">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 9h8l2 3h6v4H4z" />
                <path d="M7 16h2" />
                <path d="M14 16h2" />
                <path d="M15 12h3" />
              </svg>
            </span>
            <div>
              <h3>Export-ready Packaging</h3>
              <p>Roll packing for sea-shipping</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ==================== FACTORY CREDENTIALS ==================== -->
  <section class="section" id="factory-credentials">
    <div class="container container--narrow">
      <div class="credential-hero">
        <div class="credential-hero__copy">
          <span class="eyebrow">FACTORY CREDENTIALS &amp; COMPLIANCE</span>
          <h2>Factory Credentials &amp; Compliance</h2>
          <p>Business license verified, project-related test documents available on request.</p>
        </div>

        <div class="credential-hero__visual" aria-hidden="true">
          <div class="credential-hero__glow"></div>
          <div class="credential-hero__scene">
            <div class="credential-hero__document">
              <div class="credential-hero__seal"></div>
              <div class="credential-hero__lines"></div>
            </div>
            <div class="credential-hero__shield"></div>
            <div class="credential-hero__plant"></div>
          </div>
        </div>
      </div>

      <div class="credential-checklist" aria-label="Factory credentials and compliance">
        <div class="credential-item">
          <span class="credential-item__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
              <path d="M6 4h12v16H6z" />
              <path d="M8 8h8" />
              <path d="M8 12h6" />
              <path d="M9 16h6" />
            </svg>
          </span>
          <div class="credential-item__thumb" aria-hidden="true"></div>
          <div>
            <h3>Business License</h3>
            <p>China fabric factory entity</p>
          </div>
        </div>

        <div class="credential-item">
          <span class="credential-item__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
              <path d="M7 4h8l4 4v12H7z" />
              <path d="M15 4v4h4" />
              <path d="M9 11h6" />
              <path d="M9 14h4" />
            </svg>
          </span>
          <div class="credential-item__thumb" aria-hidden="true"></div>
          <div>
            <h3>SGS Test Reports</h3>
            <p>Available upon customer request</p>
          </div>
        </div>

        <div class="credential-item">
          <span class="credential-item__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 3l5 2v5c0 4-2.3 7.1-5 11-2.7-3.9-5-7-5-11V5z" />
              <path d="M12 8v5" />
              <path d="M10 11h4" />
            </svg>
          </span>
          <div class="credential-item__thumb" aria-hidden="true"></div>
          <div>
            <h3>Fire-Retardant Options</h3>
            <p>Available for upholstery-grade fabrics</p>
          </div>
        </div>

        <div class="credential-item">
          <span class="credential-item__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
              <path d="M6 6h12v12H6z" />
              <path d="M8 10l2 2 4-4" />
              <path d="M8 15h8" />
            </svg>
          </span>
          <div class="credential-item__thumb" aria-hidden="true"></div>
          <div>
            <h3>Batch-wise Inspection</h3>
            <p>Quality check per material batch</p>
          </div>
        </div>

        <div class="credential-item">
          <span class="credential-item__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 6h9l3 3v9H4z" />
              <path d="M7 10h6" />
              <path d="M7 13h4" />
              <path d="M15 9h5v8h-5z" />
            </svg>
          </span>
          <div class="credential-item__thumb" aria-hidden="true"></div>
          <div>
            <h3>Buyer Audit Support</h3>
            <p>Factory visit &amp; document support</p>
          </div>
        </div>
      </div>

      <div class="credential-footer">
        <div class="credential-footer__copy">
          <strong>Committed to Quality. Trusted by Global Buyers.</strong>
          <p>We welcome audits, inspections, and document verification.</p>
        </div>
        <a class="credential-footer__action" href="/contact">Request Documents</a>
      </div>
    </div>
  </section>

  <!-- ==================== OUR FACTORY ENTRY ==================== -->
  <section class="section section--alt" id="factory-entry">
    <div class="container">
      <div class="section-header">
        <span class="eyebrow">Our Factory</span>
        <h2>Our Factory</h2>
        <p>Factory-backed fabric supplier based in Guangzhou, serving Southeast Asia and the Middle East.</p>
      </div>

      <div class="grid grid--3 factory-entry-grid">
        {factoryEntries.map((entry) => (
          <a href={entry.href} class="factory-entry-card">
            <div class="factory-entry-card__visual">
              <img
                src={entry.image}
                alt={entry.alt}
                width="600"
                height="400"
                loading="lazy"
                decoding="async"
              />
              <div class="factory-entry-card__overlay">
                <span class="factory-entry-card__cta">Explore →</span>
              </div>
            </div>
            <div class="factory-entry-card__body">
              <h3>{entry.title}</h3>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ==================== FACTORY TRUST ==================== -->
  <section class="section section--dark" id="factory-trust">
    <div class="container">
      <div class="trust-layout">
        <!-- Left: content -->
        <div class="trust-layout__left">
          <span class="eyebrow">Factory & Certification</span>
          <h2>Factory-backed fabric supplier based in Guangzhou</h2>
          <p>
            Built for Southeast Asia and Middle East sourcing teams. Stable supply, clear QC,
            and direct factory support for custom projects.
          </p>

          <div class="trust-badges">
            {trustBadges.map((badge) => (
              <div class="trust-badge">
                <svg class="trust-badge__check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                  <polyline points="20 6 9 17 4 12" />
                </svg>
                <span class="trust-badge__label">{badge.label}</span>
              </div>
            ))}
          </div>

          <div class="trust-actions">
            <CTAButton href="/factory" variant="primary" size="lg">
              Explore Our Factory
            </CTAButton>
            <CTAButton href="/sample-policy" variant="secondary" size="lg">
              Request Samples
            </CTAButton>
          </div>
        </div>

        <!-- Right: factory image -->
        <div class="trust-layout__right">
          <div class="trust-image-grid">
            <img
              src="/image/process/hot_stamping_foil_transfer.jpg"
              alt="Factory workshop for thermal foil fabric production"
              loading="lazy"
            />
            <img
              src="/image/process/on_line_visual_inspection.jpg"
              alt="Quality inspection station for fabric production"
              loading="lazy"
            />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ==================== INQUIRY ==================== -->
  <section class="section section--alt inquiry-section" id="inquiry">
    <div class="container container--narrow">
      <div class="section-header">
        <span class="eyebrow">Inquiry</span>
        <h2>Need Custom Fabric or Free Sample? Get Your Quote Within 24h</h2>
      </div>

      <form class="inquiry-form" id="inquiry-form" novalidate>
        <div class="inquiry-grid">
          <input class="inquiry-input" name="name" type="text" placeholder="Name *" autocomplete="name" required />
          <input class="inquiry-input" name="company" type="text" placeholder="Company *" autocomplete="organization" required />
          <input class="inquiry-input" name="email" type="email" placeholder="Email *" autocomplete="email" required />
          <input class="inquiry-input" name="subject" type="text" value="Homepage inquiry" hidden />
        </div>
        <textarea class="inquiry-textarea" name="message" rows="4" placeholder="Your Requirement *" required></textarea>
        <div class="inquiry-actions">
          <button type="submit" class="btn btn--primary btn--lg inquiry-submit">Get Custom Quote</button>
          <a href="/sample-policy" class="btn btn--outline btn--lg inquiry-link">Free Sample</a>
        </div>
        <p class="inquiry-note">Low MOQ, custom color, and fast sample support.</p>
        <p class="inquiry-error" id="inquiry-error" hidden></p>
      </form>
    </div>
  </section>

  <!-- ==================== FAQ ==================== -->
  <section class="section" id="faq">
    <div class="container container--narrow">
      <div class="section-header">
        <span class="eyebrow">FAQ</span>
        <h2>Frequently Asked Questions</h2>
      </div>

      <div class="faq-list">
        {faqData.mainEntity.map((item, idx) => (
          <details class="faq-item" open={idx === 0}>
            <summary class="faq-item__question">
              <span>{item.name}</span>
              <svg class="faq-item__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="6 9 12 15 18 9" />
              </svg>
            </summary>
            <div class="faq-item__answer">
              <p>{item.acceptedAnswer.text}</p>
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ==================== FINAL CTA ==================== -->
  <section class="final-cta" id="final-cta">
    <div class="container">
      <div class="final-cta__inner">
        <span class="eyebrow final-cta__eyebrow">WORK WITH OUR FACTORY</span>
        <h2 class="final-cta__title">Need Custom Fabric or Free Sample? Get Your Quote Within 24h</h2>
        <p class="final-cta__text">Fast reply. Low MOQ. Custom color and pattern support.</p>
        <div class="final-cta__actions">
          <CTAButton href="/contact" variant="primary" size="lg">
            Get Custom Quote
          </CTAButton>
          <CTAButton href="/sample-policy" variant="secondary" size="lg">
            Free Sample
          </CTAButton>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* ==================== Fabric Series ==================== */
  .fabric-series-grid {
    gap: var(--space-2xl);
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }

  .hot-products-group + .hot-products-group {
    margin-top: var(--space-3xl);
  }

  .hot-products-group__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .hot-products-group__head h3 {
    font-size: var(--text-xl);
    color: var(--color-primary);
    margin: 0;
  }

  .hot-products-group__all {
    color: var(--color-accent-dark);
    font-weight: 700;
    font-size: var(--text-sm);
  }

  .fabric-card {
    display: block;
    text-decoration: none;
    color: inherit;
    background-color: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: all var(--transition-base);
  }

  .fabric-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .fabric-card__visual {
    aspect-ratio: 1 / 1;
    overflow: hidden;
    position: relative;
  }

  .fabric-card__tag {
    position: absolute;
    top: var(--space-sm);
    left: var(--space-sm);
    z-index: 1;
    display: inline-flex;
    padding: 0.25rem 0.6rem;
    border-radius: var(--radius-sm);
    background: var(--color-accent);
    color: var(--color-white);
    font-size: var(--text-xs);
    font-weight: 700;
  }

  .fabric-card__visual img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .fabric-card:hover .fabric-card__visual img {
    transform: scale(1.04);
  }

  .fabric-card__body {
    padding: var(--space-xl);
  }

  .fabric-card__body h3 {
    font-size: var(--text-xl);
    margin-bottom: var(--space-sm);
    color: var(--color-primary);
  }

  .fabric-card__body p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.7;
    margin-bottom: var(--space-md);
  }

  .fabric-card__meta {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-xs);
    font-size: var(--text-xs);
    color: var(--color-text-secondary);
  }

  .fabric-card__meta span {
    padding: 0.35rem 0.55rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
  }

  .fabric-card__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: fit-content;
    padding: 0.6rem 1rem;
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-white);
    background: var(--color-accent);
    border-radius: var(--radius-sm);
  }

  .fabric-card:hover .fabric-card__cta {
    background: var(--color-accent-dark);
  }

  /* ==================== Applications ==================== */
  .applications-grid {
    gap: var(--space-lg);
  }

  .app-card {
    display: block;
    text-decoration: none;
    color: inherit;
    border-radius: var(--radius-md);
    overflow: hidden;
    background-color: var(--color-white);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }

  .app-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .app-card__visual {
    aspect-ratio: 16 / 9;
    position: relative;
    overflow: hidden;
  }

  .app-card__visual img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .app-card:hover .app-card__visual img {
    transform: scale(1.05);
  }

  .app-card__overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding-bottom: var(--space-md);
    background: linear-gradient(to top, rgba(22, 41, 77, 0.7) 0%, transparent 50%);
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .app-card:hover .app-card__overlay {
    opacity: 1;
  }

  .app-card__cta {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-white);
    background-color: var(--color-accent);
    padding: 0.5rem 1.25rem;
    border-radius: var(--radius-full);
  }

  .app-card__body {
    padding: var(--space-md) var(--space-lg);
    text-align: center;
  }

  .app-card__body h3 {
    font-size: var(--text-lg);
    margin-bottom: 0.25rem;
  }

  .app-card__body p {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }

  .app-card__link {
    display: inline-flex;
    margin-top: var(--space-sm);
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-accent);
  }

  /* ==================== Why Nixia ==================== */
  #why-nixia {
    background: #f8f9fa;
  }

  #why-nixia .eyebrow {
    color: var(--color-accent);
    font-size: 0.72rem;
    letter-spacing: 0.14em;
  }

  .why-nixia-split {
    display: grid;
    grid-template-columns: 55fr 45fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .why-nixia-media {
    position: relative;
  }

  .why-nixia-media img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-lg);
    display: block;
  }

  .why-nixia-badges {
    position: absolute;
    left: var(--space-md);
    right: var(--space-md);
    bottom: var(--space-md);
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .why-nixia-badge-chip {
    display: inline-flex;
    align-items: center;
    min-height: 2rem;
    padding: 0.4rem 0.7rem;
    border-radius: var(--radius-full);
    background: rgba(255, 255, 255, 0.9);
    color: var(--color-primary);
    font-size: var(--text-xs);
    font-weight: 700;
    box-shadow: var(--shadow-sm);
    border: 1px solid rgba(196, 160, 53, 0.24);
  }

  .why-nixia-copy {
    text-align: left;
  }

  .why-nixia-copy h2,
  .why-nixia-advantage h3 {
    color: var(--color-primary);
  }

  .why-nixia-copy h2 {
    margin-bottom: var(--space-md);
  }

  .why-nixia-copy p,
  .why-nixia-advantage p {
    color: var(--color-text-muted);
  }

  .why-nixia-advantages {
    display: grid;
    gap: 1rem;
    margin-top: var(--space-xl);
  }

  .why-nixia-advantage {
    display: flex;
    gap: 0.9rem;
    align-items: flex-start;
    padding-bottom: 1rem;
    border-bottom: 1px solid rgba(196, 160, 53, 0.16);
  }

  .why-nixia-advantage:last-child {
    border-bottom: 0;
    padding-bottom: 0;
  }

  .why-nixia-advantage__icon {
    width: 24px;
    height: 24px;
    color: var(--color-accent);
    flex-shrink: 0;
    margin-top: 1px;
  }

  .why-nixia-advantage__icon svg {
    width: 24px;
    height: 24px;
  }

  .why-nixia-advantage h3 {
    font-size: var(--text-base);
    font-weight: 700;
    color: var(--color-primary);
    margin: 0 0 0.25rem;
  }

  .why-nixia-advantage p {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.55;
    margin: 0;
  }

  /* ==================== Manufacturing Capabilities ==================== */
  #manufacturing-capabilities {
    background: #f7f5ef;
  }

  .capabilities-shell {
    background: rgba(255, 255, 255, 0.78);
    border-radius: 28px;
    padding: clamp(1.5rem, 2vw, 2.2rem);
  }

  .capabilities-shell__title {
    text-align: center;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    color: var(--color-text-muted);
    margin-bottom: var(--space-xl);
  }

  .capability-bar {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 0;
  }

  .capability-item {
    min-width: 0;
    padding: 0 1rem;
    text-align: center;
  }

  .capability-item + .capability-item {
    border-left: 1px solid rgba(196, 160, 53, 0.12);
  }

  .capability-item:last-child {
    border-right: 0;
  }

  .capability-item__icon {
    width: 56px;
    height: 56px;
    margin: 0 auto 0.9rem;
    display: grid;
    place-items: center;
    background: #fbf2db;
    border-radius: 16px;
    color: var(--color-accent);
  }

  .capability-item__icon svg {
    width: 24px;
    height: 24px;
  }

  .capability-item h3 {
    font-size: var(--text-sm);
    line-height: 1.3;
    margin: 0 0 0.25rem;
    color: var(--color-primary);
    font-weight: 700;
    text-align: center;
  }

  .capability-item p {
    font-size: var(--text-xs);
    line-height: 1.5;
    margin: 0;
    color: var(--color-text-muted);
    text-align: center;
  }

  .capability-item > div {
    min-width: 0;
  }

  /* ==================== Factory Credentials ==================== */
  #factory-credentials {
    background: #f7f7f4;
  }

  .credential-hero {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: var(--space-3xl);
    align-items: center;
    margin-bottom: var(--space-2xl);
  }

  .credential-hero__copy {
    text-align: left;
  }

  .credential-hero__copy h2 {
    margin: 0.35rem 0 0.6rem;
    color: var(--color-primary);
  }

  .credential-hero__copy p {
    max-width: 42ch;
    color: var(--color-text-muted);
  }

  .credential-hero__visual {
    position: relative;
    min-height: 340px;
  }

  .credential-hero__glow {
    position: absolute;
    inset: 10% 0 0 0;
    border-radius: 28px;
    background: linear-gradient(90deg, rgba(255, 255, 255, 0.95) 0%, rgba(250, 248, 242, 0.85) 46%, rgba(196, 160, 53, 0.08) 100%);
    filter: blur(0.2px);
  }

  .credential-hero__scene {
    position: absolute;
    inset: 0;
    overflow: hidden;
    border-radius: 28px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.2), rgba(255, 255, 255, 0.75)),
      radial-gradient(circle at 70% 30%, rgba(255, 255, 255, 0.95) 0%, rgba(255, 255, 255, 0.15) 38%, transparent 40%),
      linear-gradient(120deg, rgba(10, 32, 72, 0.12), rgba(10, 32, 72, 0.02));
}

  .credential-hero__scene::before {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(250, 250, 247, 0.96) 0%, rgba(250, 250, 247, 0.72) 40%, rgba(250, 250, 247, 0) 100%);
  }

  .credential-hero__document {
    position: absolute;
    right: 10%;
    bottom: 10%;
    width: 44%;
    aspect-ratio: 3 / 4;
    background: #fffdf8;
    border: 3px solid rgba(196, 160, 53, 0.8);
    box-shadow: 0 18px 40px rgba(22, 41, 77, 0.16);
    transform: rotate(-4deg);
  }

  .credential-hero__seal {
    position: absolute;
    left: 50%;
    top: 9%;
    width: 26px;
    height: 26px;
    transform: translateX(-50%);
    border-radius: 50%;
    background: radial-gradient(circle, #d9b44a 0 30%, #b88a1c 31% 100%);
  }

  .credential-hero__lines {
    position: absolute;
    left: 12%;
    right: 12%;
    top: 22%;
    bottom: 12%;
    background:
      repeating-linear-gradient(180deg, rgba(22, 41, 77, 0.42) 0 1px, transparent 1px 8px),
      linear-gradient(180deg, transparent 0 12%, rgba(196, 160, 53, 0.08) 12% 13%, transparent 13% 100%);
  }

  .credential-hero__shield {
    position: absolute;
    left: 16%;
    bottom: 12%;
    width: 86px;
    height: 102px;
    background: linear-gradient(180deg, #f8df95 0%, #d2a632 45%, #9a7318 100%);
    clip-path: polygon(50% 0, 92% 16%, 92% 54%, 50% 100%, 8% 54%, 8% 16%);
    box-shadow: 0 10px 24px rgba(22, 41, 77, 0.16);
  }

  .credential-hero__shield::after {
    content: "";
    position: absolute;
    inset: 18% 18% 20%;
    border: 2px solid rgba(255, 248, 220, 0.9);
    clip-path: inherit;
  }

  .credential-hero__plant {
    position: absolute;
    right: 2%;
    bottom: 8%;
    width: 74px;
    height: 104px;
    background: radial-gradient(circle at 50% 20%, #5f9d5a 0 18%, transparent 19%),
      radial-gradient(circle at 65% 35%, #6bad66 0 18%, transparent 19%),
      radial-gradient(circle at 38% 38%, #5c8f52 0 18%, transparent 19%),
      linear-gradient(180deg, transparent 0 55%, #d8dad4 55% 100%);
  }

  .credential-checklist {
    display: grid;
    gap: 0.85rem;
  }

  .credential-item {
    display: grid;
    grid-template-columns: 96px 44px minmax(0, 1fr);
    gap: 1rem;
    align-items: center;
    padding: 1rem 1.1rem;
    border: 1px solid rgba(22, 41, 77, 0.06);
    border-radius: var(--radius-md);
    background: rgba(255, 255, 255, 0.75);
  }

  .credential-item:last-child {
    border-bottom: 1px solid rgba(22, 41, 77, 0.06);
  }

  .credential-item__icon {
    width: 64px;
    height: 64px;
    color: var(--color-accent);
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: rgba(196, 160, 53, 0.12);
  }

  .credential-item__icon svg {
    width: 24px;
    height: 24px;
  }

  .credential-item__thumb {
    width: 44px;
    height: 58px;
    border-radius: var(--radius-sm);
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.95), rgba(247, 247, 243, 0.95)),
      repeating-linear-gradient(180deg, rgba(196, 160, 53, 0.14) 0 1px, transparent 1px 7px);
    border: 1px solid rgba(196, 160, 53, 0.18);
    flex-shrink: 0;
  }

  .credential-item > div:last-child {
    padding-left: 0.2rem;
  }

  .credential-footer {
    margin-top: var(--space-2xl);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-xl);
    padding: 1.2rem 1.4rem;
    border-radius: 20px;
    background: linear-gradient(90deg, #173a6a 0%, #0f2b53 100%);
    color: var(--color-white);
  }

  .credential-footer__copy strong {
    display: block;
    font-size: var(--text-lg);
    line-height: 1.35;
    margin-bottom: 0.35rem;
  }

  .credential-footer__copy p {
    margin: 0;
    color: rgba(255, 255, 255, 0.78);
  }

  .credential-footer__action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1.25rem;
    border: 1px solid rgba(255, 205, 90, 0.8);
    border-radius: var(--radius-md);
    color: #ffcf72;
    text-decoration: none;
    font-weight: 700;
    white-space: nowrap;
  }

  /* ==================== Factory Entry ==================== */
  .factory-entry-grid {
    gap: var(--space-lg);
  }

  .factory-entry-card {
    display: block;
    text-decoration: none;
    color: inherit;
    border-radius: var(--radius-md);
    overflow: hidden;
    background-color: var(--color-white);
    border: 1px solid var(--color-border);
    transition: all var(--transition-base);
  }

  .factory-entry-card:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .factory-entry-card__visual {
    aspect-ratio: 3 / 2;
    position: relative;
    overflow: hidden;
  }

  .factory-entry-card__visual img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--transition-slow);
  }

  .factory-entry-card:hover .factory-entry-card__visual img {
    transform: scale(1.05);
  }

  .factory-entry-card__overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding-bottom: var(--space-md);
    background: linear-gradient(to top, rgba(22, 41, 77, 0.7) 0%, transparent 50%);
    opacity: 0;
    transition: opacity var(--transition-base);
  }

  .factory-entry-card:hover .factory-entry-card__overlay {
    opacity: 1;
  }

  .factory-entry-card__cta {
    font-size: var(--text-sm);
    font-weight: 700;
    color: var(--color-white);
    background-color: var(--color-accent);
    padding: 0.5rem 1.25rem;
    border-radius: var(--radius-full);
  }

  .factory-entry-card__body {
    padding: var(--space-md) var(--space-lg);
    text-align: center;
  }

  .factory-entry-card__body h3 {
    font-size: var(--text-lg);
    margin: 0;
    color: var(--color-primary);
  }

  /* ==================== Factory Trust ==================== */
  .trust-layout {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: var(--space-4xl);
    align-items: center;
  }

  .trust-layout__left h2 {
    color: var(--color-white);
    margin-bottom: var(--space-md);
  }

  .trust-layout__left > p {
    color: rgba(255, 255, 255, 0.7);
    font-size: var(--text-lg);
    line-height: 1.7;
    margin-bottom: var(--space-xl);
  }

  .trust-badges {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sm);
    margin-bottom: var(--space-xl);
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: var(--space-xs);
    padding: 0.5rem 1rem;
    border: 1px solid rgba(196, 160, 53, 0.3);
    border-radius: var(--radius-full);
    background: rgba(196, 160, 53, 0.05);
  }

  .trust-badge__check {
    width: 16px;
    height: 16px;
    color: var(--color-accent);
    flex-shrink: 0;
  }

  .trust-badge__label {
    font-size: var(--text-sm);
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
  }

  .trust-actions {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .trust-layout__right {
    position: relative;
  }

  .trust-image-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-md);
  }

  .trust-image-grid img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    aspect-ratio: 1 / 1;
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-xl);
  }

  /* ==================== Inquiry ==================== */
  .inquiry-form {
    background: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-xl);
  }

  .inquiry-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-sm);
    margin-bottom: var(--space-sm);
  }

  .inquiry-input,
  .inquiry-textarea {
    width: 100%;
    padding: 0.85rem 0.95rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    font: inherit;
    color: var(--color-text);
    background: var(--color-white);
  }

  .inquiry-textarea {
    min-height: 120px;
    resize: vertical;
  }

  .inquiry-input:focus,
  .inquiry-textarea:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(196, 160, 53, 0.12);
  }

  .inquiry-actions {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
    margin-top: var(--space-md);
  }

  .inquiry-submit,
  .inquiry-link {
    min-width: 180px;
    justify-content: center;
  }

  .inquiry-note {
    margin-top: var(--space-sm);
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }

  .inquiry-error {
    margin-top: var(--space-sm);
    color: #b91c1c;
    font-size: var(--text-sm);
  }

  /* ==================== FAQ ==================== */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
  }

  .faq-item {
    background-color: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: border-color var(--transition-base);
  }

  .faq-item[open] {
    border-color: var(--color-accent-light);
  }

  .faq-item__question {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    padding: var(--space-lg) var(--space-xl);
    font-size: var(--text-base);
    font-weight: 600;
    color: var(--color-primary);
    cursor: pointer;
    list-style: none;
    user-select: none;
  }

  .faq-item__question::-webkit-details-marker {
    display: none;
  }

  .faq-item__icon {
    width: 20px;
    height: 20px;
    color: var(--color-accent);
    flex-shrink: 0;
    transition: transform var(--transition-base);
  }

  .faq-item[open] .faq-item__icon {
    transform: rotate(180deg);
  }

  .faq-item__answer {
    padding: 0 var(--space-xl) var(--space-lg);
  }

  .faq-item__answer p {
    font-size: var(--text-sm);
    color: var(--color-text-secondary);
    line-height: 1.7;
  }

  /* ==================== Final CTA ==================== */
  .final-cta {
    background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary) 100%);
    padding-block: var(--space-5xl);
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .final-cta::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 20% 50%, rgba(196, 160, 53, 0.08) 0%, transparent 50%),
      radial-gradient(circle at 80% 50%, rgba(196, 160, 53, 0.06) 0%, transparent 50%);
  }

  .final-cta__inner {
    position: relative;
    z-index: 1;
    max-width: 720px;
    margin-inline: auto;
  }

  .final-cta__eyebrow {
    color: var(--color-accent);
    margin-bottom: var(--space-md);
  }

  .final-cta__title {
    color: var(--color-white);
    font-size: var(--text-4xl);
    margin-bottom: var(--space-md);
  }

  .final-cta__text {
    color: rgba(255, 255, 255, 0.75);
    font-size: var(--text-lg);
    line-height: 1.7;
    margin-bottom: var(--space-2xl);
  }

  .final-cta__actions {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ==================== Hero Trust Bar ==================== */
  .hero-trust {
    background-color: var(--color-primary-dark);
    padding-block: var(--space-2xl);
    border-bottom: 1px solid rgba(196, 160, 53, 0.1);
  }

  .hero-trust__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-lg);
    text-align: center;
  }

  .hero-trust__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-xs);
  }

  .hero-trust__value {
    font-size: var(--text-3xl);
    font-weight: 800;
    color: var(--color-accent);
    line-height: 1.2;
  }

  .hero-trust__label {
    font-size: var(--text-sm);
    color: rgba(255, 255, 255, 0.65);
    font-weight: 500;
    letter-spacing: 0.02em;
  }

  /* ==================== Responsive ==================== */
  @media (max-width: 968px) {
    .trust-layout {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }

    .grid--4.applications-grid,
    .grid--3.factory-entry-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .advantage-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .fabric-series-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .final-cta__title {
      font-size: var(--text-3xl);
    }

    .inquiry-grid {
      grid-template-columns: 1fr;
    }

    .trust-image-grid {
      grid-template-columns: 1fr;
    }

    .why-nixia-split {
      grid-template-columns: 1fr;
    }

    .why-nixia-media {
      order: 2;
    }

    .why-nixia-copy {
      order: 1;
    }

    .why-nixia-badges {
      left: var(--space-sm);
      right: var(--space-sm);
      bottom: var(--space-sm);
    }

    .capability-bar {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .capability-item {
      padding: 0 0.75rem;
    }

    .credential-item {
      grid-template-columns: 24px 44px minmax(0, 1fr);
    }

    .credential-hero {
      grid-template-columns: 1fr;
    }

    .credential-hero__visual {
      min-height: 280px;
    }

    .credential-footer {
      flex-direction: column;
      align-items: flex-start;
    }
  }

  @media (max-width: 640px) {
    .hero-trust__grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-xl) var(--space-lg);
    }

    .grid--4.applications-grid,
    .grid--3.factory-entry-grid,
    .grid--4.fabric-series-grid,
    .advantage-grid {
      grid-template-columns: 1fr;
    }

    .final-cta__title {
      font-size: var(--text-2xl);
    }

    .final-cta__actions {
      flex-direction: column;
    }

    .trust-actions {
      flex-direction: column;
    }

    .inquiry-submit,
    .inquiry-link {
      width: 100%;
    }

    .capability-bar {
      grid-template-columns: 1fr;
    }

    .capability-item {
      padding: 0;
    }

    .capability-item + .capability-item {
      border-left: 0;
    }

    .why-nixia-badges {
      position: static;
      margin-top: var(--space-sm);
    }

    .credential-item {
      grid-template-columns: 24px 36px minmax(0, 1fr);
      padding: 0.9rem 0.85rem;
    }

    .credential-item__thumb {
      width: 36px;
      height: 48px;
    }

    .credential-item__icon {
      width: 52px;
      height: 52px;
    }

    .credential-footer {
      padding: 1rem;
      border-radius: 16px;
    }
  }
</style>

<script>
  const inquiryForm = document.getElementById("inquiry-form") as HTMLFormElement | null;
  const inquiryError = document.getElementById("inquiry-error") as HTMLElement | null;

  function showInquiryError(message: string) {
    if (!inquiryError) return;
    inquiryError.textContent = message;
    inquiryError.hidden = false;
  }

  function hideInquiryError() {
    if (inquiryError) inquiryError.hidden = true;
  }

  inquiryForm?.addEventListener("submit", async (e) => {
    e.preventDefault();
    hideInquiryError();

    const data = new FormData(inquiryForm);
    const name = String(data.get("name") || "").trim();
    const company = String(data.get("company") || "").trim();
    const email = String(data.get("email") || "").trim();
    const message = String(data.get("message") || "").trim();

    if (!name || !company || !email || !message) {
      showInquiryError("Please complete all required fields.");
      return;
    }

    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
      showInquiryError("Please enter a valid email address.");
      return;
    }

    try {
      const res = await fetch("/api/contact", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          name,
          company,
          email,
          subject: "Homepage inquiry",
          message,
        }),
      });

      const payload = await res.json().catch(() => null);
      if (res.ok && payload?.success) {
        window.location.href = "/thank-you/";
        return;
      }

      showInquiryError(payload?.error || "Request failed. Please try again.");
    } catch {
      showInquiryError("Unable to send inquiry right now.");
    }
  });
</script>
